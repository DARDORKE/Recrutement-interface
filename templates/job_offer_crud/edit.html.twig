{% extends 'base.html.twig' %}

{% block title %}Modification de l'offre d'emploi{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
                <h1 class="h3 mb-3 fw-bolder text-center">Modification de l'offre d'emploi</h1>
        </div>
        <div class="d-flex justify-content-center">
            {{ include('job_offer_crud/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
        </div>
        <a class="d-flex justify-content-center" href="{{ path('app_login') }}">Retour à la liste</a>
        </div>
    </div>

{% endblock %}
